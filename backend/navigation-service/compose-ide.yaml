# Docker Compose for IDE Profile - Navigation Service
# Minimal PostgreSQL setup for individual module development

version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: navigation-service-postgres-ide
    environment:
      POSTGRES_DB: conversation_ui
      POSTGRES_USER: conversation_user
      POSTGRES_PASSWORD: conversation_pass
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8"
    ports:
      - "5433:5432"  # Different port to avoid conflicts with management service
    volumes:
      - postgres_data_ide:/var/lib/postgresql/data
      - ./src/main/resources/db/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U conversation_user -d conversation_ui"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks:
      - navigation-ide-network

volumes:
  postgres_data_ide:
    driver: local

networks:
  navigation-ide-network:
    driver: bridge
