// Mock configuration from PRD Section 4.3.1
export const mockConfig = {
  enabled: true, // Always use mocks for MVP
  delay: 500, // Simulated network delay in ms
  failureRate: 0, // 0-1, probability of request failure
};
